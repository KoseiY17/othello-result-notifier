name: Check Othello Results

on:
  schedule:
    - cron: "2/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check new results
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: python check_results.py

      - name: Save result state
        run: |
          if git diff --quiet known_results.json 2>/dev/null; then
            echo "No changes."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add known_results.json
            git commit -m "Update known Othello results"
            git push
          fi
